services:
  content-viewer:
    build: .
    image: content-viewer:local
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - content-viewer-content:/app/content

volumes:
  content-viewer-content:
