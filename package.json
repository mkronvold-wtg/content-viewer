{
  "name": "content-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "theme:update": "node scripts/update-theme.mjs",
    "build": "npm run theme:update && npm run check",
    "check": "node --check server.mjs && node --check extension.mjs"
  },
  "dependencies": {
    "mermaid": "^11.12.1"
  }
}
